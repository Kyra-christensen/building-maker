export function makeStatsString(waterfrontCount, skylineCount, castleCount) {
    return `You clicked on the waterfront ${waterfrontCount} times, on the skyline ${skylineCount} times, and on the castle ${castleCount} times. Everybody loves your city's slogan:`;
}